{
    "tasks": [
        {
            "label": "Bundle",
            "type": "shell",
            "command": ["rm -Rf ${workspaceFolder}/dist/* && node ${workspaceFolder}/esbuild.mjs"],
            "problemMatcher": [],
        },
        {
            "label": "Deploy",
            "type": "shell",
            "dependsOn": "Bundle",
            "command": ["pip install -r ${workspaceFolder}/deployment/requirements.txt && python ${workspaceFolder}/deployment/deploy.py"],
            "problemMatcher": [],
        }
    ]

}